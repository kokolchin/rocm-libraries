// Insert into frontend/include/hipdnn_frontend/Graph.hpp
// Add this case to the deserializeFromFlatBuffer() switch statement:
                case hipdnn_data_sdk::data_objects::NodeAttributes::{{ op.fbs_table }}:
                {
                    auto attr = {{ op.frontend.attributes_class }}::fromFlatBuffer(
                        fbNode->{{ op.frontend.deserialization_accessor }}(), tensorMap);
                    if(fbNode->name() != nullptr)
                    {
                        attr.set_name(fbNode->name()->str());
                    }
                    _sub_nodes.emplace_back(
                        std::make_shared<{{ op.frontend.node_class }}>(std::move(attr), graph_attributes));
                    break;
                }
